<div class="sixteen wide column">
	<table id="releases" class="ui large table">
		<%= render 'home/releases' %>
	</table>
	<div class="ui hidden divider"></div>
	<div id="pagination">
	  <%= link_to_next_page(@releases, 'Show More', :remote => true, class: 'fluid ui button more-button') %>
	</div>
</div>