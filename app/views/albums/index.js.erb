$("<%=j render @albums %>").appendTo($("#albums"));
// Update pagination link
<% if @albums.last_page? %>
  $('#pagination').remove();
<% else %>
  $('#pagination')
    .html("<%=j link_to_next_page(@albums, 'Show More', :remote => true, class: 'btn btn-default btn-lg btn-block') %>");
<% end %>