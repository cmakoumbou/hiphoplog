<div id="albums" class="list-group">
	<%= render @albums %>
</div>
<p id="pagination">
  <%= link_to_next_page(@albums, 'Show More', :remote => true, class: "btn btn-default btn-lg btn-block") %>
</p>