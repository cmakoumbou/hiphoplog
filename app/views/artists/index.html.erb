<% provide(:title, 'Artists') %>

<div class="sixteen wide column">
	<h2 class="ui center aligned header red header">Artists</h2>
	<table id="artists" class="ui large table">
		<%= render @artists %>
	</table>
	<div class="ui hidden divider"></div>
	<div id="pagination">
	  <%= link_to_next_page(@artists, 'Show More', :remote => true, class: "fluid ui button more-button") %>
	</div>
</div>