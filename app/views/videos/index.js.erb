$("<%=j render @videos %>").appendTo($("#videos"));
// Update pagination link
<% if @videos.last_page? %>
  $('#pagination').remove();
<% else %>
  $('#pagination')
    .html("<%=j link_to_next_page(@videos, 'Show More', :remote => true, class: 'fluid ui button') %>");
<% end %>