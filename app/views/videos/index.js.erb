$("<%=j render @videos %>").appendTo($("#videos"));
// Update pagination link
<% if @videos.last_page? %>
  $('#pagination').remove();
<% else %>
  $('#pagination')
    .html("<%=j link_to_next_page(@videos, 'Load More', :remote => true, class: 'btn btn-default btn-lg btn-block') %>");
<% end %>